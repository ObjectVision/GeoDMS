*[[Relational functions]] overlay32*

## syntax

- overlay32(*a*, *domainunit*, *container*)

## definition

overlay32 works like the [[overlay]] function, only it results in unit with [[value type]] uint32.