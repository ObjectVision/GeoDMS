# Revisions that only moved bytes around and should never show up in git blame.
#
# GitHub reads this file automatically. For local blame, point git at it once:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# Renormalise every text blob to LF (`git add --renormalize .` under `* text=auto`).
# 741 files, 213867 lines, line endings only.
fd05263971ecc61d5522b93633d39ff25486a345
